const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const Data=require('./reading/reading-data.js');const Contract=require('./reading/reading-contract.js');const catalogs=Data.createCatalogs(JSON.parse(fs.readFileSync('./data/cards-major.json')),JSON.parse(fs.readFileSync('./data/spreads.json')));
test('builds upright request',()=>{const r=Contract.buildRequest({question:'  今日どうする？  ',cards:[{cardId:'major-21',positionId:'message'}]},catalogs);assert.equal(r.question,'今日どうする？');assert.equal(r.cards[0].orientation,'upright')});
test('rejects reversed in phase one',()=>assert.throws(()=>Contract.buildRequest({cards:[{cardId:'major-21',positionId:'message',orientation:'reversed'}]},catalogs)));
test('result exposes only public contract',()=>{const req=Contract.buildRequest({cards:[{cardId:'major-21',positionId:'message'}]},catalogs);const result=Contract.validateResult({schemaVersion:1,readingId:'r',mode:'fallback',summary:'要約',interpretation:'解釈',cardReadings:[{positionId:'message',position:'今日の一枚',cardId:'major-21',card:'世界',orientation:'upright',reading:'読み'}],advice:'助言',cautions:[],provider:'secret',fallbackReason:'timeout'},req);assert.equal(result.provider,undefined);assert.equal(result.fallbackReason,undefined)});
