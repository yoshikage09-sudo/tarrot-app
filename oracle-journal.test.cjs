const test=require('node:test');const assert=require('node:assert/strict');const Journal=require('./oracle-journal-store.js');
function memory(initial=null){let value=initial;return {getItem(){return value},setItem(k,v){value=v}}}
const legacy={id:'old',createdAt:'2026-09-23T00:00:00Z',cardId:1,theme:'未分類',memo:'',reading:{title:'魔術師',message:'旧形式',action:'一歩'}};
const structured={id:'new',createdAt:'2026-09-23T01:00:00Z',cardId:21,theme:'仕事・学び',memo:'確認',reading:{schemaVersion:1,readingId:'new',mode:'ai',summary:'要約',interpretation:'解釈',cardReadings:[{cardId:'major-21'}],advice:'助言',cautions:[]},readingContext:{question:'仕事について',spreadId:'one-oracle',cards:[{cardId:'major-21',positionId:'message',orientation:'upright'}]}};
test('loads legacy version one records unchanged',()=>{const j=Journal.createJournal(memory(JSON.stringify({version:1,entries:[legacy]})));assert.equal(j.list()[0].reading.message,'旧形式')});
test('structured reading and context survive reload',()=>{const store=memory();Journal.createJournal(store).save(structured);const loaded=Journal.createJournal(store).list()[0];assert.equal(loaded.reading.mode,'ai');assert.equal(loaded.readingContext.cards[0].orientation,'upright')});
